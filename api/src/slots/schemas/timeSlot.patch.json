{
  "$id": "/schemas/timeSlot.patch",
  "$async": true,
  "http_method": "PATCH",
  "http_path": "/time_slots/:id",
  "title": "TimeSlot",
  "description": "TimeSlot schema",
  "type": "object",
  "properties": {
    "started_at": {
      "type": "string",
      "format": "date-time"
    },
    "ended_at": {
      "type": "string",
      "format": "date-time"
    },
    "customer_id": {
      "anyOf": [
        {
          "type": "integer",
          "exclusiveMinimum": 0
        },
        {
          "type": "string",
          "format": "uuid"
        }
      ]
    },
    "slot_id": {
      "anyOf": [
        {
          "type": "integer",
          "exclusiveMinimum": 0
        },
        {
          "type": "string",
          "format": "uuid"
        }
      ]
    }
  },
  "additionalProperties": false
}